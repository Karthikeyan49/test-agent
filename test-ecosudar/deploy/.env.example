# Copy to .env and fill in real values (the real .env is git-ignored).
DB_HOST=db
DB_PORT=3306
DB_NAME=ecosudar_test
DB_USER=ecosudar
DB_PASS=CHANGE_ME
DB_CHARSET=utf8mb4
CORS_ORIGIN=http://localhost:8080
# 64-char hex; generate with: php -r "echo bin2hex(random_bytes(32));"
JWT_SECRET=CHANGE_ME_64_HEX
APP_ENV=development
